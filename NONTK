-- [[ BLOX FRUITS FULL AUTO COMBO & M1 ANNIHILATOR ]]
-- Platform: PC & Mobile (Delta, Codex, Arceus X, Wave, Solara)
-- Feature: Melee -> Sword -> Fruit Skill Rotation + Cooldown M1 Spam + Aimlock

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")
local VirtualInputManager = game:GetService("VirtualInputManager")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

-- // CONFIGURATION
local Config = {
    AutoCombo = false,
    AutoWarp = false,
    SpectateTarget = false,
    ComboMode = "FullCombo", -- "FullCombo" (หมัด->ดาบ->ผล) หรือ "M1Spam" (หมัดรัวอย่างเดียว)
    AttackDistance = 3.0,
    AttackHeight = 1.5,
    SelectedTarget = nil,
    SafeZoneCheck = true,
    M1Speed = 0.03,          -- ความเร็วในการสับ M1
    SkillDelay = 0.25        -- ดีเลย์ระหว่างกดแต่ละสกิล
}

-- // TARGET STATS & SAFE ZONE
local function IsPlayerInSafeZone(player)
    if not player or not player.Character then return true end
    local char = player.Character
    if char:FindFirstChildOfClass("ForceField") then return true end
    if char:FindFirstChild("SafeZone") or char:FindFirstChild("InSafeZone") then return true end
    local data = player:FindFirstChild("Data")
    if data and data:FindFirstChild("Level") and data.Level.Value < 20 then return true end
    return false
end

local function GetTargetHealth(player)
    if not player or not player.Character then return "DEAD", 0, 0 end
    local hum = player.Character:FindFirstChildOfClass("Humanoid")
    if not hum then return "DEAD", 0, 0 end
    local hp = math.floor(hum.Health)
    local maxHp = math.floor(hum.MaxHealth)
    return hp > 0 and (hp .. "/" .. maxHp) or "DEAD", hp, maxHp
end

local function GetTargetLevel(player)
    local data = player:FindFirstChild("Data")
    if data and data:FindFirstChild("Level") then return data.Level.Value end
    local ls = player:FindFirstChild("leaderstats")
    if ls and ls:FindFirstChild("Level") then return ls.Level.Value end
    return "???"
end

local function GetDistance(player)
    if not LocalPlayer.Character or not LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then return 99999 end
    if not player.Character or not player.Character:FindFirstChild("HumanoidRootPart") then return 99999 end
    return math.floor((LocalPlayer.Character.HumanoidRootPart.Position - player.Character.HumanoidRootPart.Position).Magnitude)
end

-- // WEAPON MANAGER
local function GetToolByType(category)
    local bp = LocalPlayer:FindFirstChild("Backpack")
    local char = LocalPlayer.Character
    if not bp or not char then return nil end

    local allTools = {}
    for _, t in pairs(char:GetChildren()) do if t:IsA("Tool") then table.insert(allTools, t) end end
    for _, t in pairs(bp:GetChildren()) do if t:IsA("Tool") then table.insert(allTools, t) end end

    for _, tool in pairs(allTools) do
        local tType = tool:GetAttribute("WeaponType") or tool.ToolTip or ""
        local name = string.lower(tool.Name)
        category = string.lower(category)

        if category == "melee" then
            if tool.ToolTip == "Melee" or string.find(name, "combat") or string.find(name, "karate") or 
               string.find(name, "superhuman") or string.find(name, "godhuman") or string.find(name, "dragon") or 
               string.find(name, "claw") or string.find(name, "electric") or string.find(name, "death step") then
                return tool
            end
        elseif category == "sword" then
            if tool.ToolTip == "Sword" or string.find(name, "blade") or string.find(name, "katana") or 
               string.find(name, "saber") or string.find(name, "scythe") or string.find(name, "dagger") or 
               string.find(name, "sword") or string.find(name, "yoru") or string.find(name, "tushita") or string.find(name, "yama") then
                return tool
            end
        elseif category == "fruit" then
            if tool.ToolTip == "Blox Fruit" or string.find(name, "fruit") or string.find(name, "dough") or 
               string.find(name, "leopard") or string.find(name, "portal") or string.find(name, "dragon") or 
               string.find(name, "kitsune") or string.find(name, "t-rex") or string.find(name, "venom") or string.find(name, "buddha") then
                return tool
            end
        end
    end
    return nil
end

local function EquipTool(tool)
    if not tool or not LocalPlayer.Character then return end
    local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
    if hum and tool.Parent ~= LocalPlayer.Character then
        hum:EquipTool(tool)
    end
end

-- // AIMLOCK & KEYBOARD PRESS SIMULATOR
local function AimAtTarget(targetPlayer)
    if not targetPlayer or not targetPlayer.Character then return end
    local myHrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    local tHrp = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
    if myHrp and tHrp then
        myHrp.CFrame = CFrame.new(myHrp.Position, Vector3.new(tHrp.Position.X, myHrp.Position.Y, tHrp.Position.Z))
    end
end

local function PressKey(keyCode)
    pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end

-- // FAST M1 CLICKER
local function ClickM1()
    if not LocalPlayer.Character then return end
    local tool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
    if tool then tool:Activate() end

    pcall(function()
        VirtualUser:CaptureController()
        VirtualUser:Button1Down(Vector2.new(0, 0))
        task.wait(0.01)
        VirtualUser:Button1Up(Vector2.new(0, 0))
    end)
end

-- // COMBO EXECUTOR
local isExecutingCombo = false
local function ExecuteFullBurst(target)
    if isExecutingCombo then return end
    isExecutingCombo = true

    local melee = GetToolByType("Melee")
    local sword = GetToolByType("Sword")
    local fruit = GetToolByType("Fruit")

    -- 1. หมัดเปิดคอมโบ (Melee: Z -> X -> C)
    if melee then
        EquipTool(melee)
        task.wait(0.1)
        AimAtTarget(target)
        PressKey(Enum.KeyCode.Z); task.wait(Config.SkillDelay)
        AimAtTarget(target)
        PressKey(Enum.KeyCode.X); task.wait(Config.SkillDelay)
        AimAtTarget(target)
        PressKey(Enum.KeyCode.C); task.wait(Config.SkillDelay)
    end

    -- 2. ยัดดาบต่อ (Sword: Z -> X)
    if sword then
        EquipTool(sword)
        task.wait(0.1)
        AimAtTarget(target)
        PressKey(Enum.KeyCode.Z); task.wait(Config.SkillDelay)
        AimAtTarget(target)
        PressKey(Enum.KeyCode.X); task.wait(Config.SkillDelay)
    end

    -- 3. ยัดผลปีศาจปิดฉาก (Fruit: Z -> X -> C -> V)
    if fruit then
        EquipTool(fruit)
        task.wait(0.1)
        AimAtTarget(target)
        PressKey(Enum.KeyCode.Z); task.wait(Config.SkillDelay)
        AimAtTarget(target)
        PressKey(Enum.KeyCode.X); task.wait(Config.SkillDelay)
        AimAtTarget(target)
        PressKey(Enum.KeyCode.C); task.wait(Config.SkillDelay)
        AimAtTarget(target)
        PressKey(Enum.KeyCode.V); task.wait(Config.SkillDelay)
    end

    -- 4. สลับกลับมาถือหมัดเพื่อเตรียมรัว M1 ในช่วงคูลดาวน์
    if melee then EquipTool(melee) end
    task.wait(0.2)
    isExecutingCombo = false
end

-- // HEARTBEAT LOCK-ON (เกาะติดหลังศัตรู ไม่เด้งกลับ)
RunService.Heartbeat:Connect(function()
    if Config.AutoWarp and Config.SelectedTarget and Config.SelectedTarget.Parent == Players then
        local target = Config.SelectedTarget
        local _, hp = GetTargetHealth(target)
        local isSafe = IsPlayerInSafeZone(target)

        if hp > 0 and (not Config.SafeZoneCheck or not isSafe) then
            local myChar = LocalPlayer.Character
            local targetChar = target.Character

            if myChar and targetChar then
                local myHrp = myChar:FindFirstChild("HumanoidRootPart")
                local targetHrp = targetChar:FindFirstChild("HumanoidRootPart")

                if myHrp and targetHrp then
                    -- เกาะหลังศัตรู + หันหน้าเข้าหาเป้าหมาย
                    local lockPosition = targetHrp.CFrame * CFrame.new(0, Config.AttackHeight, Config.AttackDistance)
                    myHrp.CFrame = CFrame.new(lockPosition.Position, targetHrp.Position)
                    
                    -- Zero Velocity ป้องกันการดีด
                    myHrp.AssemblyLinearVelocity = Vector3.zero
                    myHrp.AssemblyAngularVelocity = Vector3.zero

                    -- ปิดการชนกัน
                    for _, part in pairs(myChar:GetChildren()) do
                        if part:IsA("BasePart") then part.CanCollide = false end
                    end
                end
            end
        end
    end
end)

-- // MAIN ATTACK & COMBO LOOP
task.spawn(function()
    while true do
        task.wait(Config.M1Speed)
        if Config.AutoCombo and Config.SelectedTarget and Config.SelectedTarget.Parent == Players then
            local target = Config.SelectedTarget
            local _, hp = GetTargetHealth(target)
            local isSafe = IsPlayerInSafeZone(target)

            if hp > 0 and (not Config.SafeZoneCheck or not isSafe) then
                local dist = GetDistance(target)
                if dist <= 22 then
                    if Config.ComboMode == "FullCombo" and not isExecutingCombo then
                        task.spawn(function() ExecuteFullBurst(target) end)
                    end
                    -- รัวหมัด M1 ซ้ำตลอดเวลา
                    ClickM1()
                end
            end
        end
    end
end)

-- // SPECTATE CONTROLLER
RunService.RenderStepped:Connect(function()
    if Config.SpectateTarget and Config.SelectedTarget and Config.SelectedTarget.Character then
        local hum = Config.SelectedTarget.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            Camera.CameraSubject = hum
            return
        end
    end
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
        if Camera.CameraSubject ~= LocalPlayer.Character.Humanoid and not Config.SpectateTarget then
            Camera.CameraSubject = LocalPlayer.Character.Humanoid
        end
    end
end)

-- // ==========================================
-- // TOUCH & MOUSE COMPATIBLE CLEAN UI ENGINE
-- // ==========================================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Onyx_Combo_Destroyer"
ScreenGui.ResetOnSpawn = false

pcall(function()
    ScreenGui.Parent = gethui and gethui() or (CoreGui:FindFirstChild("RobloxGui") or CoreGui)
end)
if not ScreenGui.Parent then ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui") end

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 360, 0, 480)
MainFrame.Position = UDim2.new(0.5, -180, 0.5, -240)
MainFrame.BackgroundColor3 = Color3.fromRGB(14, 14, 18)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

local Stroke = Instance.new("UIStroke", MainFrame)
Stroke.Color = Color3.fromRGB(130, 40, 255)
Stroke.Thickness = 1.6

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
Title.Text = "⚡ ONYX AUTO COMBO & M1 ANNIHILATOR"
Title.TextColor3 = Color3.fromRGB(230, 230, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 12
Title.Parent = MainFrame
Instance.new("UICorner", Title).CornerRadius = UDim.new(0, 10)

-- Mobile Floating Icon
local FloatBtn = Instance.new("TextButton")
FloatBtn.Size = UDim2.new(0, 50, 0, 50)
FloatBtn.Position = UDim2.new(0, 12, 0.45, 0)
FloatBtn.BackgroundColor3 = Color3.fromRGB(130, 40, 255)
FloatBtn.Text = "⚔️"
FloatBtn.TextSize = 22
FloatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FloatBtn.Parent = ScreenGui
Instance.new("UICorner", FloatBtn).CornerRadius = UDim.new(1, 0)
FloatBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

-- Target Stats Card
local StatCard = Instance.new("Frame")
StatCard.Size = UDim2.new(0.92, 0, 0, 110)
StatCard.Position = UDim2.new(0.04, 0, 0, 48)
StatCard.BackgroundColor3 = Color3.fromRGB(20, 20, 26)
StatCard.Parent = MainFrame
Instance.new("UICorner", StatCard).CornerRadius = UDim.new(0, 8)

local TargetLabel = Instance.new("TextLabel", StatCard)
TargetLabel.Size = UDim2.new(1, -14, 0, 22)
TargetLabel.Position = UDim2.new(0, 8, 0, 6)
TargetLabel.Text = "Target: [ None ]"
TargetLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TargetLabel.Font = Enum.Font.GothamBold
TargetLabel.TextSize = 13
TargetLabel.TextXAlignment = Enum.TextXAlignment.Left

local InfoLabel = Instance.new("TextLabel", StatCard)
InfoLabel.Size = UDim2.new(1, -14, 0, 75)
InfoLabel.Position = UDim2.new(0, 8, 0, 28)
InfoLabel.Text = "Level: -- | HP: --\nDistance: -- studs\nZone: --\nAttack State: IDLE"
InfoLabel.TextColor3 = Color3.fromRGB(180, 180, 200)
InfoLabel.Font = Enum.Font.Gotham
InfoLabel.TextSize = 12
InfoLabel.TextXAlignment = Enum.TextXAlignment.Left
InfoLabel.TextYAlignment = Enum.TextYAlignment.Top

-- Player Selector
local Scroll = Instance.new("ScrollingFrame")
Scroll.Size = UDim2.new(0.92, 0, 0, 140)
Scroll.Position = UDim2.new(0.04, 0, 0, 168)
Scroll.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
Scroll.ScrollBarThickness = 3
Scroll.Parent = MainFrame
Instance.new("UICorner", Scroll).CornerRadius = UDim.new(0, 8)

local Layout = Instance.new("UIListLayout", Scroll)
Layout.Padding = UDim.new(0, 3)

local function MakeBtn(txt, pos, cb)
    local b = Instance.new("TextButton")
    b.Size = UDim2.new(0.44, 0, 0, 36)
    b.Position = pos
    b.BackgroundColor3 = Color3.fromRGB(28, 28, 36)
    b.Text = txt
    b.TextColor3 = Color3.fromRGB(240, 240, 255)
    b.Font = Enum.Font.GothamBold
    b.TextSize = 12
    b.Parent = MainFrame
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 6)
    b.MouseButton1Click:Connect(function() cb(b) end)
    return b
end

-- Controls Grid
local WarpToggle = MakeBtn("Lock Warp: OFF", UDim2.new(0.04, 0, 0, 318), function(b)
    Config.AutoWarp = not Config.AutoWarp
    b.Text = "Lock Warp: " .. (Config.AutoWarp and "ON" or "OFF")
    b.BackgroundColor3 = Config.AutoWarp and Color3.fromRGB(130, 40, 255) or Color3.fromRGB(28, 28, 36)
end)

local ComboToggle = MakeBtn("Auto Combo: OFF", UDim2.new(0.52, 0, 0, 318), function(b)
    Config.AutoCombo = not Config.AutoCombo
    b.Text = "Auto Combo: " .. (Config.AutoCombo and "ON" or "OFF")
    b.BackgroundColor3 = Config.AutoCombo and Color3.fromRGB(240, 50, 50) or Color3.fromRGB(28, 28, 36)
end)

local ModeToggle = MakeBtn("Mode: Full Burst", UDim2.new(0.04, 0, 0, 362), function(b)
    if Config.ComboMode == "FullCombo" then
        Config.ComboMode = "M1Spam"
        b.Text = "Mode: M1 Only"
    else
        Config.ComboMode = "FullCombo"
        b.Text = "Mode: Full Burst"
    end
end)

local SpyToggle = MakeBtn("Spy Cam: OFF", UDim2.new(0.52, 0, 0, 362), function(b)
    Config.SpectateTarget = not Config.SpectateTarget
    b.Text = "Spy Cam: " .. (Config.SpectateTarget and "ON" or "OFF")
    b.BackgroundColor3 = Config.SpectateTarget and Color3.fromRGB(0, 170, 110) or Color3.fromRGB(28, 28, 36)
end)

local Refresh = MakeBtn("🔄 Refresh Player List", UDim2.new(0.04, 0, 0, 410), function() end)
Refresh.Size = UDim2.new(0.92, 0, 0, 34)

local function UpdateList()
    for _, c in pairs(Scroll:GetChildren()) do
        if c:IsA("TextButton") then c:Destroy() end
    end
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(1, -6, 0, 30)
            btn.BackgroundColor3 = (Config.SelectedTarget == p) and Color3.fromRGB(130, 40, 220) or Color3.fromRGB(25, 25, 32)
            local hp = GetTargetHealth(p)
            local lvl = GetTargetLevel(p)
            btn.Text = string.format(" %s | Lv.%s | HP: %s", p.DisplayName, tostring(lvl), tostring(hp))
            btn.TextColor3 = Color3.fromRGB(240, 240, 240)
            btn.Font = Enum.Font.Gotham
            btn.TextSize = 11
            btn.TextXAlignment = Enum.TextXAlignment.Left
            btn.Parent = Scroll
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)

            btn.MouseButton1Click:Connect(function()
                Config.SelectedTarget = p
                UpdateList()
            end)
        end
    end
    Scroll.CanvasSize = UDim2.new(0, 0, 0, Layout.AbsoluteContentSize.Y + 10)
end

Refresh.MouseButton1Click:Connect(UpdateList)
Players.PlayerAdded:Connect(UpdateList)
Players.PlayerRemoving:Connect(UpdateList)
UpdateList()

-- Telemetry HUD
task.spawn(function()
    while true do
        task.wait(0.25)
        if Config.SelectedTarget and Config.SelectedTarget.Parent == Players then
            local t = Config.SelectedTarget
            local hpStr, hpVal = GetTargetHealth(t)
            local lvl = GetTargetLevel(t)
            local dist = GetDistance(t)
            local inSafe = IsPlayerInSafeZone(t)

            TargetLabel.Text = "Target: " .. t.DisplayName
            InfoLabel.Text = string.format(
                "Level: %s  |  HP: %s\nDistance: %d studs\nZone: %s\nStatus: %s",
                tostring(lvl),
                hpStr,
                dist,
                inSafe and "🛡️ SAFE ZONE" or "⚔️ COMBAT ZONE",
                (hpVal > 0 and not inSafe) and (isExecutingCombo and "🔥 CASTING COMBO..." or "⚡ SPAMMING M1") or "PROTECTED/DEAD"
            )
        else
            TargetLabel.Text = "Target: [ None Selected ]"
            InfoLabel.Text = "Level: -- | HP: --\nDistance: -- studs\nZone: --\nStatus: IDLE"
        end
    end
end)
